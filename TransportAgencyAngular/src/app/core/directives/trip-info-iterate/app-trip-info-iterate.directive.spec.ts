import { AppTripInfoIterateDirective } from './app-trip-info-iterate.directive';

describe('AppTripInfoIterateDirective', () => {
  it('should create an instance', () => {
    const directive = new AppTripInfoIterateDirective();
    expect(directive).toBeTruthy();
  });
});
